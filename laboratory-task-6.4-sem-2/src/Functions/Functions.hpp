#ifndef FUNCTIONS_HPP
#define FUNCTIONS_HPP
#include <iostream>
#include <vector>
#include <string>
#include <random>

void initMtrx(std::vector<std::vector<int>>&);

void printMtrx(std::vector<std::vector<int>>);

void anihilateMax(std::vector<std::vector<int>>&);

void sortRows(std::vector<std::vector<int>>&);

#endif
